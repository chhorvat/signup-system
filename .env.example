# Admin password for the /admin section
ADMIN_PASSWORD=changeme

# Where to store the SQLite database file
DB_PATH=./data/signup.db

# The public URL of your app (used in cancel links in emails)
APP_URL=http://localhost:3000

# SMTP email settings (optional - emails will be skipped if not configured)
# SMTP_HOST=smtp.gmail.com
# SMTP_PORT=587
# SMTP_USER=yourapp@gmail.com
# SMTP_PASS=your-app-password
# EMAIL_FROM=Basketball League <yourapp@gmail.com>
